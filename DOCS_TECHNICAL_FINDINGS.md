# Docs technical findings

Every claim and sample in the integration guide (`/pt-br/documentacao`,
`/en/docs`) was checked against the code that implements it. Where the old
guide and the code disagreed, the difference is recorded here. The guide now
describes what the code does; behaviour was **not** changed to make the guide
look right.

Classification: **DOC_WRONG** — the guide was wrong or incomplete ·
**IMPLEMENTATION_WRONG** — the code does not do what a founder needs or what the
product tells them · **UNCLEAR** — needs a decision or verification against
Stripe.

| # | Class | Severity | Finding | Evidence | What the guide does now |
| --- | --- | --- | --- | --- | --- |
| T1 | IMPLEMENTATION_WRONG → **FIXED** | **High** | **A founder could not receive Stripe webhooks with the flow the product described.** Verification used one platform-wide `STRIPE_WEBHOOK_SECRET` and resolved the workspace from `event.account`, which Stripe only sets for Connect; an endpoint added in the founder's own Stripe dashboard is signed with *its own* secret → `invalid_signature`. Integrations also showed "Conectado" as soon as an `acct_…` id was typed. **Fixed with per-workspace signing secrets:** each Stripe integration has its own endpoint, `POST /api/webhooks/stripe/<integrationId>`, which loads that integration (service connection, ingest path), decrypts its `whsec_…` secret (AES-256-GCM, `integrations.encrypted_credentials`), verifies with it, and processes with the workspace fixed to the integration's — never `event.account`. Same idempotency claim in `webhook_events`, now with `workspace_id` set. Integrations is a 3-step setup (account id → copy this integration's URL and select the events → paste the signing secret, never shown back), and its status comes from evidence (`integration-health`): "Aguardando o primeiro evento", "Recebendo eventos · último …", "Último evento falhou: …" (processing failure, or a delivery whose signature did not match the saved secret). The legacy `/api/webhooks/stripe` stays for Connect and the local Stripe CLI setup. | `src/app/api/webhooks/stripe/[integrationId]/route.ts`, `src/lib/billing/stripe/webhook.ts` (`verifyStripeWebhook`), `src/server/services/integrations.ts` (`webhookTargetForIntegration`, `saveStripeWebhookSecret`), `src/server/services/billing-events.ts` (`ingestVerifiedWebhook`), `src/server/services/integration-health.ts`, `src/features/integrations/stripe-panel.tsx`; tests in `src/lib/billing/stripe/__tests__/webhook.test.ts` and `src/app/api/webhooks/stripe/[integrationId]/__tests__/route.test.ts` | Step 3 describes the new flow: copy the URL from Integrations (shown with an `INTEGRATION_ID` placeholder), select the events from the generated table, paste the signing secret, then confirm with a test event. |
| T2 | DOC_WRONG | High | The old guide never said where `visitorId` comes from, though identify requires it — the founder had no way to complete step 2 without reading the tracker source. | `src/lib/tracking/script.ts` writes `_referral_id` via `document.cookie` and exposes `window.Referral.visitorId` | New "De onde vem o visitorId" subsection: read the `_referral_id` cookie on the same domain, or send `window.Referral?.visitorId` with the sign-up form |
| T3 | DOC_WRONG / IMPLEMENTATION_WRONG → **FIXED** | High | `providerCustomerId` was optional in the identify contract but a Stripe payment found the affiliate **only** through it; identify without it recorded the customer but no payment ever earned a commission. **Fixed with an e-mail fallback:** when no customer has the payment's provider customer id (or the one that has it was created by a webhook without an `externalId`), the payment's e-mail (`invoice.customer_email`, `payment_intent.receipt_email`) is hashed and matched against `customers.email_hash` of identified customers in the same workspace — exactly one match, with no provider id or the same one. The Stripe id is then back-filled onto that customer (only if its column is empty, never over a different id) and onto its bound attributions, so later payments without an e-mail match by id. Attribution now also uses the matched customer's `externalId`. | `src/server/services/billing-events.ts` (`resolveCustomer`, `identifiedCustomerByEmail`, `recordPayment`); tests in `src/server/services/__tests__/billing-events.db.test.ts` | Field table: `providerCustomerId` is *recommended*; `email` explains it is the fallback and must be the address Stripe has. The callout says "Envie o providerCustomerId ou o e-mail" and when a payment carries an e-mail. |
| T4 | DOC_WRONG | Medium | The event list was incomplete: the adapter also handles `invoice.paid`, `payment_intent.succeeded` (one-off payments) and `customer.subscription.created`. | `src/lib/billing/stripe/adapter.ts` | The table is generated from `STRIPE_HANDLED_EVENTS`; a test compares it with the adapter's `case` labels |
| T5 | DOC_WRONG | Low | The tracker reads the referral code from `ref`, `via` **or** `aff`; the guide only mentioned `?ref=`. | `REF_QUERY_PARAMS` in `src/lib/tracking/constants.ts` | Listed, generated from the constant |
| T6 | IMPLEMENTATION_WRONG → **FIXED** | High | Refunds and disputes did not find the payment they undo: payments are stored under the invoice id (subscriptions) or the PaymentIntent id (one-off), but `charge.refunded` read `charge.invoice` (absent since API basil) and fell back to the PaymentIntent, and `charge.dispute.created` used the charge id, which never matched. A second partial refund reused `${charge.id}_refund` and was dropped as a duplicate, and a partial refund flipped the whole commission to `reversed`. **Fixed:** new table `transaction_references` (migration `0007_new_switch`) maps every id of a payment (`in_`, `pi_`, `ch_`) to the id it is recorded under, filled from the payment event itself, from expanded `invoice.payments`, and from `invoice_payment.paid` (now handled; order-independent). Refunds now come from `refund.created` (the refund's own `re_` id and amount; `charge.refunded` only carries a running total and is no longer read) and disputes use `dp_`, so each partial refund is its own transaction. The reversal is cumulative and proportional — `round_half_up(commission × refunded so far / base)` minus earlier reversals, exact integer math — so partial refunds never drift and a full refund reverses exactly the commission. The original flips to `reversed` only when refunds cover the whole payment; until then the negative row carries a payable status so a payout nets it. A `paid` commission is never rewritten: the reversal row is recorded as `reversed` and **nothing is clawed back** (open gap). | `src/lib/billing/stripe/adapter.ts`, `src/lib/billing/stripe/events.ts`, `src/lib/billing/types.ts`, `src/server/services/billing-events.ts` (`recordRefund`, `recordReferences`, `findPaymentByReferences`), `src/server/domain/commission.ts` (`calculateReversal`, `proportionHalfUp`, `planReversal`); tests in `src/lib/billing/stripe/__tests__/adapter.test.ts`, `src/server/domain/__tests__/commission.test.ts`, `src/server/services/__tests__/billing-events.db.test.ts` | The generated event table lists `invoice_payment.paid` and `refund.created` instead of `charge.refunded`, and says reversals are proportional. |
| T7 | UNCLEAR | Low | identify accepts `provider: "paddle" | "manual"`, but only Stripe is integrated; a `paddle` customer can never be matched by a Stripe webhook. | `src/lib/api/contract.ts` | Documents `provider` as optional, default `stripe`, "o único integrado hoje" |
| T8 | IMPLEMENTATION_WRONG | Low | identify error responses include `message` in English (`"Invalid API key."`); the product otherwise avoids English strings. Harmless for a server-to-server API, but inconsistent. | `src/app/api/identify/route.ts` | The guide documents only the stable `error` codes |
| T9 | DOC_WRONG | Low | The old sample used `https://app.example.com`; a copied snippet pointed nowhere. | old `docs/page.tsx` | Samples use the deployment's own origin (`NEXT_PUBLIC_APP_URL`) |
| T10 | UNCLEAR → **FIXED** | Low | A comment in `api/track/route.ts` said its `Set-Cookie` kept the visitor id "first-party" — that cookie was set on the IndicaFluxo host, not the customer's site, so it was dead. **Removed**: `/api/track` sets no cookie; the only visitor cookie is the one the tracker writes with JavaScript on the customer's own domain. | `src/app/api/track/route.ts` | The guide describes the tracker's own cookie only |
| T11 | DOC_WRONG → **FIXED** | Low | `generateApiKey`'s doc comment said "32 random bytes"; the code uses 24. The comment now says 24. | `src/lib/crypto/hash.ts` | Not mentioned |
| T12 | IMPLEMENTATION_WRONG → **FIXED** | **High** | **Test and live were one ledger.** Keys were always issued `pk_live_`/`sk_live_`, Stripe's `livemode` was never read, and one signing secret per integration meant a founder's test-mode endpoint wrote the real ledger. **Fixed (docs/PLANS.md §2):** keys are issued per environment (`pk_test_`/`sk_test_`, `pk_live_`/`sk_live_`; the row stores `environment`, and a key whose prefix disagrees with its row is invalid); `/api/track` resolves referral codes only among programs of the key's environment; `/api/identify` writes customers of the key's environment and binds only that environment's open attributions. An integration holds two encrypted secrets (`{ webhookSecret /* live */, testWebhookSecret }`), each saved and replaced on its own; a delivery is verified against both, and the event's `livemode` must match the secret that verified it (else `400 livemode_mismatch`). Normalised events carry `environment`; customers, transactions and attribution lookup are confined to it. Without live mode (Launch/Growth in good standing): live clicks → `204`, nothing written; live identify → `402 LIVE_MODE_REQUIRED` (`SUBSCRIPTION_REQUIRED` past grace); live Stripe events → `200 { received: true, ignored: "live_mode_inactive" }` **without** a `webhook_events` row, so they can be re-sent after reactivation. Live keys can only be generated with live mode. | `src/lib/crypto/hash.ts`, `src/server/services/api-keys.ts` (`rotateApiKey`, `authenticateApiKey`, `resolvePublishableKey`), `src/server/services/tracking.ts`, `src/server/services/identify.ts`, `src/lib/billing/stripe/webhook.ts` (`verifyStripeWebhookWithSecrets`), `src/server/services/integrations.ts`, `src/server/services/billing-events.ts` (`ingestVerifiedWebhook`); tests in `src/lib/crypto/__tests__/hash.test.ts`, `src/lib/billing/stripe/__tests__/webhook.test.ts`, `src/app/api/webhooks/stripe/[integrationId]/__tests__/route.test.ts`, `src/server/services/__tests__/ingest.db.test.ts`, `billing-events.db.test.ts` | New section "Teste e produção": the key pairs, the two Stripe endpoints and secrets, `livemode` routing, what happens without live mode, live mode needing Launch/Growth (links to pricing). Samples use `pk_test_`/`sk_test_`. Stripe steps say to add the endpoint in both modes. Errors table lists `LIVE_MODE_REQUIRED` and `SUBSCRIPTION_REQUIRED`. |
| T13 | IMPLEMENTATION_WRONG → **FIXED** | **High** | **One payment could earn two commissions.** Since API basil (the installed SDK is dahlia) neither a PaymentIntent nor its charge names its invoice, so `payment_intent.succeeded` and `invoice.paid` for the same subscription payment were recorded as two payments unless `invoice_payment.paid` happened to arrive between them. **Fixed with an order-independent rule:** `transaction_references` maps every id of a payment to the one id it is recorded under; every event touching a payment takes a transaction-scoped advisory lock on each id it knows (sorted), which serialises the PaymentIntent event and the invoice event against the link. A payment event whose ids point at a recorded payment records nothing; one whose ids point at a payment not yet recorded (`pi_ → in_`, link first) defers to it. The link maps every id to whatever is already recorded; if both the PaymentIntent and the invoice were recorded before it (they share no id, so no lock can prevent that), the first record is kept and the second is reversed in full by an `adjustment` transaction `dup_<id>` and a reversal row — nothing deleted, net one commission. Also fixed a race it exposed: two events creating the same new customer at once no longer fail on the unique key. | `src/server/services/billing-events.ts` ("One payment, one commission" comment, `recordPayment`, `recordReferences`, `reverseDuplicatePayment`); tests in `billing-events.db.test.ts`: PaymentIntent→link→invoice, link→PaymentIntent→invoice, both before the link in both orders plus redelivery of all three, and all three delivered concurrently (three rounds, committed) | — |
| T14 | IMPLEMENTATION_WRONG → **FIXED** | **High** | **A failed webhook was lost.** The claim was `ON CONFLICT DO NOTHING`, so Stripe's retry of a failed event answered `duplicate`. **Fixed:** the claim (`claimWebhookEvent`, scope `customer_billing`) re-claims an event whose status is `failed`, so the retry processes it. | `src/server/repositories/webhook-events.ts`, `src/server/services/billing-events.ts`; test "a failed event is claimed again" in `billing-events.db.test.ts` | "Reentregas não duplicam comissão" now also says a failed event is processed on retry. |
| T15 | IMPLEMENTATION_WRONG → **FIXED** | Medium | **A refund or dispute arriving before its payment was ignored forever.** Now it is marked `failed` with a clear message and answered `500`, so Stripe retries it once the payment is recorded; after Stripe's retry window it stays failed and visible on Integrations. A refund of a payment deliberately left out of the ledger (it had no customer) is still ignored — its ids are remembered — instead of failing for three days. | `src/server/services/billing-events.ts` (`PaymentNotRecordedYetError`, `recordRefund`); tests in `billing-events.db.test.ts` | — |
| T16 | IMPLEMENTATION_WRONG → **FIXED** | **High** | **identify answered 500 when Stripe created the customer first** (unique key on the provider id), and a re-identify without `providerCustomerId`/`email` wiped the stored ones. **Fixed:** a customer the webhook created with that provider id gets the `externalId` attached; stored provider id and e-mail hash are kept (`coalesce`), a provider id already owned by another external id is never taken over, and a concurrent insert is retried as a merge. Only open (not expired) attributions of the key's environment are bound. | `src/server/services/identify.ts`; tests in `ingest.db.test.ts` | — |
| T17 | IMPLEMENTATION_WRONG → **FIXED** | **High** | **Revoked publishable keys kept recording clicks** (`workspaceForPublishableKey` ignored `revoked_at`). Now unknown, revoked, secret and mislabelled keys all answer `401 unknown_key`. Rotating a key records `api_key.revoked` for the key it replaces. A click also never moves an attribution already bound to a customer to another affiliate (renewal lock), attribution decisions are serialised per (program, visitor), and a code shared by two programs resolves deterministically (active program first, then newest). | `src/server/services/api-keys.ts`, `src/server/services/tracking.ts`; tests in `ingest.db.test.ts` | Error `unknown_key` says the key may have been replaced. |
| T18 | IMPLEMENTATION_WRONG → **FIXED** | Medium | `/t.js` is statically generated with `NEXT_PUBLIC_APP_URL` baked in; a build without it froze `http://localhost:3000/api/track` into production. The tracker now posts to the host that served the script (`new URL("/api/track", script.src)`), with the baked URL only as a fallback. | `src/lib/tracking/script.ts`, `src/app/t.js/route.ts`; test in `src/lib/tracking/__tests__/script.test.ts` | — |
| T19 | IMPLEMENTATION_WRONG → OPEN | Low | Integrations shows the latest event per Stripe mode, but `public.latest_webhook_event(uuid)` has no environment argument and also returns `platform_billing` events that carry a `workspace_id`. Until a migration adds `latest_webhook_event(uuid, environment)` filtered to `scope = 'customer_billing'`, the per-mode line is derived from the latest transaction recorded in that environment (no failures, no non-payment events). `integration-health.ts` switches to the new function automatically once it exists and is granted to `indica_app`. | `src/server/services/integration-health.ts` (`environmentEvidence`) | — |

## Checked and correct

- Tracker tag: `<script defer src="…/t.js" data-key="pk_test_…">` (or `pk_live_…`) — path from `TRACKER_PATH`, attribute read with `getAttribute("data-key")` (tested).
- Cookie `_referral_id`, 365 days, `SameSite=Lax`, `Secure` on HTTPS; no request without a code in the URL.
- identify: `POST /api/identify`, `Authorization: Bearer` secret key only, no CORS, body schema (the example is parsed against the real schema in tests), response `{ ok, customerId, attributionsBound }`, 120 requests/minute per IP.
- Webhook path `/api/webhooks/stripe/<integrationId>`; idempotency by `(scope, provider, provider_event_id)`, a failed event re-claimable.
- Commission: 4 900 × 3 000 bps = 1 470 (the example is computed with `applyBasisPoints`, tested); half-up rounding; no commission when the participation is not approved, the currency differs from the program, the first payment is outside the attribution window, or renewals exceed the duration.
- Defaults quoted (last click, 60-day window, 30-day hold) are the new-program form defaults.
- Error codes and statuses listed are the ones the routes return.
