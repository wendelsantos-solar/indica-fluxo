import { DetailPageSkeleton } from "@/components/feedback/page-skeleton"

/** A batch's detail shape: breadcrumb bar, metric strip, items table. */
export default function PayoutBatchLoading() {
  return <DetailPageSkeleton />
}
