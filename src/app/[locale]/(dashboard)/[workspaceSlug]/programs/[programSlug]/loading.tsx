import { DetailPageSkeleton } from "@/components/feedback/page-skeleton"

/** A program's detail shape: status line, rule strip, tabs. */
export default function ProgramDetailLoading() {
  return <DetailPageSkeleton />
}
